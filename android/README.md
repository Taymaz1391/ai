# Local AI Studio Android shell

Open this directory in Android Studio and build the APK. Set `LOCAL_AI_URL` in `app/src/main/java/com/localai/studio/MainActivity.kt` to the URL of the locally running UI server.
